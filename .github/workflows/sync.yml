name: Sync
on: [push]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: git push --force https://NickClickCasas:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/NickClickCasas/Denia-Property-Care main.git
